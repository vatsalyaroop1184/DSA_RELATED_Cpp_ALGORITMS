Here i in some deletions and insertions functions i didnt consider edge cases like when header has only one node or no node at all.

-> think where u can use "node->prev" also efficiently like we can use it in del_at_end function.
cd