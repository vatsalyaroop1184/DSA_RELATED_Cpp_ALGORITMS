In this code there are three functions for same aim i.e finding majority element.
1.brute_force:
    O(n^2) is time complexity.
2.better_approch using sorting:
   O(nlog(n))[for sorting] + O(n)[for loop] = O(nlog(n) + n).
3.moore_voting algo:
  O(n) is time complexity.
  dont need sorting.

  ** in the int main() part of code in the part of scaning inputs as we didn't fix an number to size of v in continues to scan even after we "enter" to get output so we should type cntrl + Z and then enter to get output. **